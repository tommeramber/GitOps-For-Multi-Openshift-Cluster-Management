apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "machineconfigs.fullname" . }}-test-connection"
  labels:
    {{- include "machineconfigs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "machineconfigs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
