apiVersion: v1
kind: Service
metadata:
  name: {{ include "machineconfigs.fullname" . }}
  labels:
    {{- include "machineconfigs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "machineconfigs.selectorLabels" . | nindent 4 }}
