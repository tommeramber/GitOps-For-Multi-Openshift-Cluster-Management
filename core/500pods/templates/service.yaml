apiVersion: v1
kind: Service
metadata:
  name: {{ include "500pods.fullname" . }}
  labels:
    {{- include "500pods.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "500pods.selectorLabels" . | nindent 4 }}
