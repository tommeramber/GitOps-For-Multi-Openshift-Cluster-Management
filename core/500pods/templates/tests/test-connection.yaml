apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "500pods.fullname" . }}-test-connection"
  labels:
    {{- include "500pods.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "500pods.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
