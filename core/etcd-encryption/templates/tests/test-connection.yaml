apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etcd-encryption.fullname" . }}-test-connection"
  labels:
    {{- include "etcd-encryption.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etcd-encryption.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
