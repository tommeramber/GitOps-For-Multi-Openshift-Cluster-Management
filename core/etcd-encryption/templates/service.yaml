apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd-encryption.fullname" . }}
  labels:
    {{- include "etcd-encryption.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etcd-encryption.selectorLabels" . | nindent 4 }}
