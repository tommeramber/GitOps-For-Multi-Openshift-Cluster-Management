apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dnsforwarder.fullname" . }}-test-connection"
  labels:
    {{- include "dnsforwarder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dnsforwarder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
