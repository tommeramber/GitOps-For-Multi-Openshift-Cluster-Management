apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnsforwarder.fullname" . }}
  labels:
    {{- include "dnsforwarder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnsforwarder.selectorLabels" . | nindent 4 }}
