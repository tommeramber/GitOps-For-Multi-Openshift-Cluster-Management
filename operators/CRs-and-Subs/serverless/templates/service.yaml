apiVersion: v1
kind: Service
metadata:
  name: {{ include "serverless.fullname" . }}
  labels:
    {{- include "serverless.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "serverless.selectorLabels" . | nindent 4 }}
